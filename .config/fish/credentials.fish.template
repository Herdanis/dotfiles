# Fish Shell Credentials Template
#
# Copy this file to credentials.fish and fill in your actual values:
#   cp credentials.fish.template credentials.fish
#
# The credentials.fish file is gitignored and will not be committed.

# ========================================
# API Keys for AI/LLM Services
# ========================================

# OpenAI API Key (for Avante.nvim)
# Get from: https://platform.openai.com/api-keys
# set -gx OPENAI_API_KEY "sk-..."

# Anthropic API Key (for Claude in Avante.nvim)
# Get from: https://console.anthropic.com/settings/keys
# set -gx ANTHROPIC_API_KEY "sk-ant-..."

# Moonshot API Key (for Kimi in Avante.nvim)
# Get from: https://platform.moonshot.cn/
# set -gx MOONSHOT_API_KEY "..."

# ========================================
# Cloud Provider Credentials
# ========================================

# Google Cloud (if needed beyond gcloud auth)
# set -gx GOOGLE_APPLICATION_CREDENTIALS "/path/to/service-account.json"

# AWS Credentials
# set -gx AWS_ACCESS_KEY_ID "..."
# set -gx AWS_SECRET_ACCESS_KEY "..."
# set -gx AWS_DEFAULT_REGION "us-east-1"

# Azure
# set -gx AZURE_SUBSCRIPTION_ID "..."
# set -gx AZURE_TENANT_ID "..."

# DigitalOcean
# set -gx DIGITALOCEAN_ACCESS_TOKEN "..."

# ========================================
# Development & CI/CD
# ========================================

# GitHub Personal Access Token
# Get from: https://github.com/settings/tokens
# set -gx GITHUB_TOKEN "ghp_..."

# GitLab Personal Access Token
# set -gx GITLAB_TOKEN "..."

# Docker Hub / Container Registry
# set -gx DOCKER_USERNAME "..."
# set -gx DOCKER_PASSWORD "..."

# ========================================
# Database Credentials
# ========================================

# PostgreSQL
# set -gx PGHOST "localhost"
# set -gx PGPORT "5432"
# set -gx PGUSER "postgres"
# set -gx PGPASSWORD "..."
# set -gx PGDATABASE "myapp"

# MySQL
# set -gx MYSQL_HOST "localhost"
# set -gx MYSQL_USER "root"
# set -gx MYSQL_PASSWORD "..."

# MongoDB
# set -gx MONGO_URI "mongodb://localhost:27017/myapp"

# ========================================
# Other Services
# ========================================

# Terraform Cloud
# set -gx TF_TOKEN_app_terraform_io "..."

# Pulumi
# set -gx PULUMI_ACCESS_TOKEN "..."

# Kubernetes contexts (if needed)
# set -gx KUBECONFIG "$HOME/.kube/config"

# ========================================
# Custom Environment Variables
# ========================================

# Add your custom environment variables below
# set -gx MY_CUSTOM_VAR "value"

# ========================================
# Notes
# ========================================
#
# Security Best Practices:
# 1. Never commit credentials.fish to git
# 2. Use environment-specific credentials
# 3. Rotate keys regularly
# 4. Use service accounts where possible
# 5. Consider using secret management tools (1Password, Vault, etc.)
#
# Fish Shell Variable Scopes:
# - set -gx : Global environment variable (exported to child processes)
# - set -g  : Global variable (Fish only)
# - set -Ux : Universal variable (persists across sessions)
# - set -l  : Local variable (function scope)
#
# Testing:
# After setting variables, test with:
#   echo $OPENAI_API_KEY
#   env | grep API_KEY
